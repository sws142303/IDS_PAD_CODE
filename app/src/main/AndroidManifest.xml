<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.azkj.pad.activity">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- 腾讯bugly权限 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_LOGS" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- 添加访问网络的权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 允许访问振动设备(Allows access to the vibrator) -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 添加随意拖动的桌面悬浮窗口 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- 允许程序修改全局音频 -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- 录音录制声音通过手机或耳机的麦克 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- 开机启动 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- 屏幕保持唤醒 不锁屏 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 禁用键盘锁允许程序禁用键盘锁 -->
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <!-- 允许一个程序获取信息有关当前或最近运行的任务,一个缩略的任务状态,是否活动等等 -->
    <uses-permission android:name="android.permission.GET_TASKS" />
    <!-- 允许程序读取底层系统日志文件 -->
    <uses-permission android:name="android.permission.READ_LOGS" />
    <!-- 允许程序访问有关GSM网络信息 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 允许一个程序访问CellID或WiFi热点来获取粗略的位置 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 允许一个程序访问精良位置(如 GPS) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- 允许程序访问Wi-Fi网络状态信息 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 改变网络状态如是否能联网 改变WiFi多播状态 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- 读取电话状态访问电话状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 用来设置了内置sd卡的写权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 允许挂载和反挂载文件系统 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <!-- 允许程序读取或写入系统设置 -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <!-- 读取联系人权限 -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <!-- 添加联系人权限 -->
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <!-- 拨打电话权限 -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <!-- 使用摄像头权限 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 蓝牙权限 -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <!-- 删除后台运行进程权限 -->
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />
    <!-- 官博权限 -->
    <uses-permission android:name="android.permission.BROADCAST_STICKY" />
    <!--<uses-permission android:name="android.permission.ACCESS_MOCK_LOCATION" />-->

    <uses-feature android:name="android.hardware.camera.flash" />

    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    <!-- 摄像头 -->
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <!-- 百度地图权限 -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />
    <uses-permission android:name="android.permission.MANAGE_ACCOUNTS" />
    <uses-permission android:name="android.permission.AUTHENTICATE_ACCOUNTS" />
    <uses-permission android:name="com.android.launcher.permission.READ_SETTINGS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_LOGS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <supports-screens android:anyDensity="true" />
    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />


    <application
        android:name=".PTT_3G_PadApplication"
        android:allowBackup="true"
        android:configChanges="orientation|keyboardHidden"
        android:excludeFromRecents="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <!-- 腾讯bugly -->
        <!-- 配置APP ID -->
        <meta-data
            android:name="BUGLY_APPID"
            android:value="1bbdffb181" />
        <!-- 配置APP版本号 -->
        <meta-data
            android:name="BUGLY_APP_VERSION"
            android:value="3.5.5.1" />
        <!-- 配置APP渠道号 -->
        <meta-data
            android:name="BUGLY_APP_CHANNEL"
            android:value="1" />
        <!-- 配置Bugly调试模式（true或者false） -->
        <meta-data
            android:name="BUGLY_ENABLE_DEBUG"
            android:value="true" />
        <meta-data
            android:name="com.baidu.lbsapi.API_KEY"
            android:value="ELmvyRQ8Ty75LZmMTtgAMrSC4klybgPN" />

        <!--
             <meta-data
                    android:name="com.baidu.lbsapi.API_KEY"
                    android:value="	ELmvyRQ8Ty75LZmMTtgAMrSC4klybgPN"/>
        -->
        <service
            android:name="com.baidu.location.f"
            android:enabled="true"
            android:excludeFromRecents="true"
            android:process=":remote" />
        <!-- 更新对讲组信息服务 -->
        <service
            android:name="com.azkj.pad.service.PTTService"
            android:excludeFromRecents="true" />

        <!-- 登录页面 -->
        <activity
            android:name=".LoginActivity"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:screenOrientation="landscape"
            android:theme="@style/AppSplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 主页面 -->
        <!-- 必须开启硬件加速hardwareAccelerated否则多视频无法onSurfaceTextureAvailable -->
        <activity
            android:name=".MainActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:excludeFromRecents="true"
            android:hardwareAccelerated="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:screenOrientation="landscape" />
        <!-- 对讲页面 -->
        <activity
            android:name=".IntercomActivity"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:screenOrientation="landscape" />
        <!-- 通话页面 -->
        <activity
            android:name=".CallingActivity"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:screenOrientation="landscape" />
        <!-- 会议页面 -->
        <activity
            android:name=".MeetingActivity"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:screenOrientation="landscape" />
        <!-- 消息页面 -->
        <activity
            android:name=".MessageActivity"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="adjustPan" />
        <!-- 通讯录页面 -->
        <activity
            android:name=".ContactActivity"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:screenOrientation="landscape" />
        <!-- 定位页面 -->
        <activity
            android:name=".PositionActivity"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:screenOrientation="landscape" />
        <!-- 监控页面 -->
        <!-- 监控页面 -->
        <activity
            android:name=".MonitorsActivity"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:screenOrientation="landscape" />
        <!-- 配置页面 -->
        <activity
            android:name=".SettingActivity"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:screenOrientation="landscape" />
        <!-- 短消息录制视频 -->
        <activity
            android:name=".RecoderActivity"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:screenOrientation="landscape" />
        <!-- 短消息发送位置 -->
        <activity
            android:name=".MyLocationActivity"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:screenOrientation="landscape" />
        <!-- 短消息显示图片 -->
        <activity
            android:name=".MessageImageShowActivity"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:screenOrientation="landscape" />
        <!-- 短消息显示视频 -->
        <activity
            android:name=".PlayViewActivity"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:screenOrientation="landscape" />
        <activity
            android:name="com.azkj.pad.photoalbum.ShowImageActivity"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:screenOrientation="landscape" />
        <activity
            android:name="com.azkj.pad.photoalbum.ShowPhotoActivity"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:screenOrientation="landscape" />
        <!-- &lt;!&ndash; 等待对话框 &ndash;&gt; -->
        <!-- <activity -->
        <!-- android:name="com.azkj.pad.activity.WaitingDialog" -->
        <!-- android:excludeFromRecents="true" -->
        <!-- android:label="@string/app_name" -->
        <!-- android:screenOrientation="landscape" -->
        <!-- android:theme="@android:style/Theme.Dialog" > -->
        <!-- </activity> -->
        <!-- &lt;!&ndash; 监控选项框 &ndash;&gt; -->
        <!-- <activity -->
        <!-- android:name="com.azkj.pad.activity.MonitorDialog" -->
        <!-- android:excludeFromRecents="true" -->
        <!-- android:label="@string/app_name" -->
        <!-- android:screenOrientation="landscape" -->
        <!-- android:theme="@android:style/Theme.Dialog" > -->
        <!-- </activity> -->
        <!-- &lt;!&ndash; 自定义对话框 &ndash;&gt; -->
        <!-- <activity -->
        <!-- android:name="com.azkj.pad.activity.CustomDialog" -->
        <!-- android:excludeFromRecents="true" -->
        <!-- android:label="@string/app_name" -->
        <!-- android:screenOrientation="landscape" -->
        <!-- android:theme="@android:style/Theme.Dialog" > -->
        <!-- </activity> -->
        <!-- 通知栏 -->
        <activity
            android:name=".NotifyActivity"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:screenOrientation="landscape" />

        <!-- 离线地图下载 -->
        <activity
            android:name=".OfflineMapActivity"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:launchMode="singleInstance"
            android:screenOrientation="landscape"
            android:theme="@style/MyDialogStyleBottom" />

        <!-- 消息广播接收器 -->
        <receiver android:name="com.azkj.pad.service.MessageReceiver">
            <intent-filter>
                <action android:name="android.intent.action.NEW_MESSAGE" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MESSAGE_RESULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <!-- 开机启动广播接收器 -->
        <receiver android:name="com.azkj.pad.service.BootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />

                <category android:name="android.intent.category.HOME" />
            </intent-filter>
        </receiver>
        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".ShowVideoActivity" />
        <activity android:name="com.azkj.pad.photoalbum.ShowVideosActivity" />
    </application>

</manifest>